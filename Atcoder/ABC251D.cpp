#include <bits/stdc++.h>
#define FOR(i, a, b) for (int i = (a); i <= (b); ++i)

using namespace std;

int main() {
    cout << 300 << endl;
    FOR(i, 1, 100) cout << i << endl;
    FOR(i, 1, 100) cout << 100 * i << endl;
    FOR(i, 1, 100) cout << 100 * 100 * i << endl;
    return 0;
}